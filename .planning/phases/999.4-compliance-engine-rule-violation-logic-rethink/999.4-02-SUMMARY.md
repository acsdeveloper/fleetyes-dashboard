---
plan: 999.4-02
status: complete
---

# Summary: Date Attribution Fix + Prospective Check Cleanup

## What was done

### Date attribution fixes (assimilated.ts)
- `EU_WEEKLY_DRIVE_LIMIT`: violation/warning `date` now uses `attributionDate` (last day with driving in week) instead of `ws` (Monday)
- `EU_FORTNIGHTLY_DRIVE_LIMIT`: violation/warning `date` now uses `fortAttributionDate` (last day in fortnight) instead of `ws`
- `EU_WEEKLY_REST` + `EU_CONSECUTIVE_REDUCED_WEEKLY_REST`: violation `date` now uses `firstWorkingDay` (first non-rest day in deficient week) instead of `ws`

### Prospective check (prospective-check.ts)
- Removed all 3 console.warn diagnostic blocks (lines ~193-199, ~229-231, ~309-314)
- Added `tripId`, `tripStartTime`, `tripEndTime` to all 5 violation/warning push objects

## key-files
- apps/v4/lib/compliance-engine/assimilated.ts — 3 date attribution fixes
- apps/v4/lib/compliance-engine/prospective-check.ts — warn removal + trip context

## Test results
All 71 tests pass — no regressions.

## Self-Check: PASSED

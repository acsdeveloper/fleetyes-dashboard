---
id: "999.9-03"
wave: 3
autonomous: true
depends_on: ["999.9-01"]
objective: "Replace all hardcoded English strings in medium and low priority pages — fleet-management, import-hub, org-settings, drivers, holidays, parking, off-shift, fleets, maintenance-trips, maintenance, inventory, vehicles, places, calendar, rota, settings, issues."
files_modified:
  - apps/v4/app/(dashboard)/fleet-management/page.tsx
  - apps/v4/app/(dashboard)/import-hub/page.tsx
  - apps/v4/app/(dashboard)/org-settings/page.tsx
  - apps/v4/app/(dashboard)/drivers/page.tsx
  - apps/v4/app/(dashboard)/holidays/page.tsx
  - apps/v4/app/(dashboard)/parking/page.tsx
  - apps/v4/app/(dashboard)/off-shift/page.tsx
  - apps/v4/app/(dashboard)/fleets/page.tsx
  - apps/v4/app/(dashboard)/maintenance-trips/page.tsx
  - apps/v4/app/(dashboard)/maintenance/page.tsx
  - apps/v4/app/(dashboard)/inventory/page.tsx
  - apps/v4/app/(dashboard)/vehicles/page.tsx
  - apps/v4/app/(dashboard)/places/page.tsx
  - apps/v4/app/(dashboard)/calendar/page.tsx
  - apps/v4/app/(dashboard)/rota/page.tsx
  - apps/v4/app/(dashboard)/settings/page.tsx
  - apps/v4/app/(dashboard)/issues/page.tsx
  - apps/v4/components/lang-context.tsx
task_count: 4
---

# Plan 999.9-03: Replace Hardcoded Strings — Medium & Low Priority Pages

## Objective

Replace hardcoded strings in all remaining pages. Three pages have NO
useLang() at all (fleet-management, import-hub, org-settings) and need
the import + hook added. The rest have partial coverage — wire up the gaps.

**Dependency:** Plan 999.9-01 must be complete.

---

## Task 1 — Wire up pages with NO translation (fleet-management, import-hub, org-settings)

<read_first>
- `apps/v4/app/(dashboard)/fleet-management/page.tsx` (full file)
- `apps/v4/app/(dashboard)/import-hub/page.tsx` (full file)
- `apps/v4/app/(dashboard)/org-settings/page.tsx` (full file)
- `apps/v4/components/lang-context.tsx` — `fleetManagement`, `importHub`, `orgSettings` namespaces (added in plan 999.9-01)
</read_first>

<action>
For each of these three pages:

1. **Add import** at the top of the file (after existing imports):
   ```typescript
   import { useLang } from "@/components/lang-context"
   ```

2. **Add hook call** inside each component function that renders user-visible text:
   ```typescript
   const { t } = useLang()
   ```

3. **Replace every hardcoded JSX string** with the corresponding `t.fleetManagement.xxx`, `t.importHub.xxx`, or `t.orgSettings.xxx` key from the new namespace created in Plan 999.9-01.

   For generic strings already in `t.common.*` (Save, Cancel, Search, etc.):
   - Use `t.common.save`, `t.common.cancel`, `t.common.search`, etc.

   For page-specific strings:
   - Use the new namespace keys.

Commit per page:
- `feat(i18n-999.9): add translation to fleet-management page`
- `feat(i18n-999.9): add translation to import-hub page`
- `feat(i18n-999.9): add translation to org-settings page`
</action>

<acceptance_criteria>
- `fleet-management/page.tsx` contains `import { useLang }` and `t.fleetManagement`
- `import-hub/page.tsx` contains `import { useLang }` and `t.importHub`
- `org-settings/page.tsx` contains `import { useLang }` and `t.orgSettings`
- None of the 3 files contain `"Save"` or `"Cancel"` as raw JSX text (use t.common)
</acceptance_criteria>

---

## Task 2 — Replace remaining strings in drivers and people-module pages

<read_first>
- `apps/v4/app/(dashboard)/drivers/page.tsx` (full file)
- `apps/v4/app/(dashboard)/holidays/page.tsx` (full file)
- `apps/v4/app/(dashboard)/off-shift/page.tsx` (full file)
- `apps/v4/app/(dashboard)/fleets/page.tsx` (full file)
- `apps/v4/components/lang-context.tsx` — `t.drivers`, `t.holidays`, `t.offShift` namespaces
</read_first>

<action>
For each "people" page that already uses `useLang()`:

**drivers/page.tsx:**
- Replace hardcoded add/edit drawer field labels with `t.drivers.xxx` keys
- Replace delete confirmation text with `t.drivers.deleteConfirm`
- Replace toast messages with `t.drivers.saveSuccess`, `t.drivers.saveError`, etc.
- Column headers not in `t.common` → `t.drivers.xxx`
- Commit: `feat(i18n-999.9): replace hardcoded strings in drivers page`

**holidays/page.tsx:**
- Replace remaining filter/drawer field labels with `t.holidays.xxx` or `t.common.xxx`
- Commit: `feat(i18n-999.9): replace hardcoded strings in holidays page`

**off-shift/page.tsx:**
- Replace remaining labels with `t.offShift.xxx` or `t.common.xxx`
- Commit: `feat(i18n-999.9): replace hardcoded strings in off-shift page`

**fleets/page.tsx:**
- Replace drawer field labels with `t.common.xxx` where applicable
- Commit: `feat(i18n-999.9): replace hardcoded strings in fleets page`

**If any string requires a new key not yet in lang-context.tsx:**
Add the key to the `Translations` type AND all 6 language objects in lang-context.tsx,
then commit lang-context.tsx separately:
`feat(i18n-999.9): add missing translation keys for people pages`
</action>

<acceptance_criteria>
- `drivers/page.tsx` hardcoded string count significantly reduced
- `holidays/page.tsx`, `off-shift/page.tsx`, `fleets/page.tsx` — all remaining gaps wired up
- Any new lang-context.tsx additions cover all 6 languages
</acceptance_criteria>

---

## Task 3 — Replace remaining strings in operational pages

<read_first>
- `apps/v4/app/(dashboard)/parking/page.tsx` (full file)
- `apps/v4/app/(dashboard)/vehicles/page.tsx` (full file)
- `apps/v4/app/(dashboard)/places/page.tsx` (full file)
- `apps/v4/app/(dashboard)/maintenance-trips/page.tsx` (full file)
- `apps/v4/app/(dashboard)/maintenance/page.tsx` (full file)
- `apps/v4/app/(dashboard)/inventory/page.tsx` (full file)
- `apps/v4/components/lang-context.tsx` — `t.parking`, `t.vehicles`, `t.places`, `t.maintenance`, `t.maintenanceHub`, `t.inventoryHub`
</read_first>

<action>
For each page, replace remaining hardcoded strings:

**parking/page.tsx:**
- Extend `t.parking` with any missing column headers, empty states, filter labels
- Replace all with `t.parking.xxx` or `t.common.xxx`
- Commit: `feat(i18n-999.9): replace hardcoded strings in parking page`

**vehicles/page.tsx:**
- Wire up remaining drawer field labels with `t.vehicles.xxx` or `t.common.xxx`
- Commit: `feat(i18n-999.9): replace hardcoded strings in vehicles page`

**places/page.tsx:**
- Replace remaining labels with `t.places.xxx` or `t.common.xxx`
- Commit: `feat(i18n-999.9): replace hardcoded strings in places page`

**maintenance/page.tsx and maintenance-trips/page.tsx:**
- Replace remaining labels with `t.maintenance.xxx` or `t.maintenanceHub.xxx`
- Commit: `feat(i18n-999.9): replace hardcoded strings in maintenance pages`

**inventory/page.tsx:**
- Replace remaining labels with `t.inventoryHub.xxx`
- Commit: `feat(i18n-999.9): replace hardcoded strings in inventory page`
</action>

<acceptance_criteria>
- All 5 pages have reduced hardcoded string counts
- `parking/page.tsx` uses `t.parking.xxx` for parking-specific text
- `vehicles/page.tsx` uses `t.vehicles.xxx` for vehicle-specific text
- Any new lang-context additions cover all 6 languages
</acceptance_criteria>

---

## Task 4 — Replace remaining strings in light-gap pages

<read_first>
- `apps/v4/app/(dashboard)/calendar/page.tsx` (full file)
- `apps/v4/app/(dashboard)/rota/page.tsx` (full file)
- `apps/v4/app/(dashboard)/settings/page.tsx` (full file)
- `apps/v4/app/(dashboard)/issues/page.tsx` (full file)
- `apps/v4/app/(dashboard)/(dashboard)/page.tsx` (dashboard index, full file)
- `apps/v4/components/lang-context.tsx` — relevant namespaces
</read_first>

<action>
These pages have 2–5 hardcoded strings each. Wire them up:

**calendar/page.tsx:** Replace remaining 5 strings with `t.calendar.xxx`
**rota/page.tsx:** Replace remaining 4 strings with `t.rota.xxx`
**settings/page.tsx:** Replace remaining 3 strings with appropriate `t.xxx` keys
**issues/page.tsx:** Replace remaining 3 strings with `t.issues.xxx` or `t.common.xxx`
**Dashboard index:** Replace remaining 2 strings with `t.common.xxx`

Commit all in one: `feat(i18n-999.9): replace remaining hardcoded strings in light-gap pages`

Add any missing lang-context keys with all 6 language values.
</action>

<acceptance_criteria>
- `calendar/page.tsx`, `rota/page.tsx`, `settings/page.tsx`, `issues/page.tsx` have 0 remaining hardcoded JSX text strings
- Dashboard index has 0 remaining hardcoded strings beyond template interpolations
- `npx tsc --noEmit` shows no new errors from these changes
</acceptance_criteria>

---
id: "999.9-01"
wave: 1
autonomous: true
objective: "Extend lang-context.tsx with all new typed translation keys needed across every dashboard page — covering fuelReceipts, tollReceipts, fuelTracking, drivers, fleet-management, import-hub, org-settings, and compliance namespaces — and populate all 6 languages (en/de/fr/es/it/pl) for every new key."
files_modified:
  - apps/v4/components/lang-context.tsx
task_count: 5
---

# Plan 999.9-01: Extend lang-context.tsx — All New Translation Keys

## Objective

Add all missing translation keys to `lang-context.tsx` covering strings
hardcoded in dashboard pages. This is the foundation wave — all other
plans depend on these keys existing before they can be referenced.

**Rules (MANDATORY):**
- Only EXTEND — never rename or remove existing keys
- Add to `Translations` type first, then add to ALL 6 language objects
- All 6 langs: `en | de | fr | es | it | pl`
- Machine translations for non-English are acceptable
- Keys must be fully typed — no `any`, no optional where required

---

## Task 1 — Read current lang-context.tsx fully

<read_first>
- `apps/v4/components/lang-context.tsx` (full file — ALL 2375 lines)
</read_first>

<action>
Read the entire file to understand:
1. The `Translations` type shape (lines 1–336)
2. The existing `en` language object (lines 340–end of en block)

Do NOT write anything yet — just understand the structure.
</action>

<acceptance_criteria>
- Agent has read and understood existing namespace boundaries
- Agent has confirmed existing keys that must not be changed
</acceptance_criteria>

---

## Task 2 — Extend fuelReceipts and tollReceipts namespaces in Translations type

<read_first>
- `apps/v4/components/lang-context.tsx` lines 195–205 (existing fuelReceipts and tollReceipts type entries)
- `apps/v4/app/(dashboard)/fuel/page.tsx` lines 175–410 (AddReceiptsModal and ReceiptFilterDrawer)
- `apps/v4/app/(dashboard)/toll/page.tsx` lines 184–410 (AddReceiptsModal, ReceiptFilterDrawer) and lines 477–650 (SendToAmazonModal)
</read_first>

<action>
In the `Translations` type (around line 195–205), REPLACE:

```typescript
  fuelReceipts: {
    captured: string; supplier: string; product: string; duplicate: string; receipt: string; uploadZip: string
    searchPlaceholder: string
  }
```

WITH:

```typescript
  fuelReceipts: {
    captured: string; supplier: string; product: string; duplicate: string; receipt: string; uploadZip: string
    searchPlaceholder: string
    // AddReceiptsModal
    addModalTitle: string; addModalSubtitle: string
    dropTitle: string; dropActive: string; dropHint: string
    filesSelected: string
    stepZipping: string; stepUploading: string; stepImporting: string; stepProcessing: string
    progressZipping: string; progressUploading: string; progressImporting: string; progressProcessing: string
    processingNote: string
    successTitle: string; successCount: string
    errorTitle: string
    btnAdd: string; btnUpload: string; btnDone: string; btnTryAgain: string; btnClose: string
    // ReceiptFilterDrawer
    filterTitle: string; anyDriver: string; anyStatus: string; capturedFrom: string; capturedTo: string
    // Receipt detail drawer
    detailTitle: string; ocrData: string; rawText: string
    colOcrStatus: string; colCapturedAt: string; colSupplier: string; colProduct: string; colVolume: string
  }
```

AND REPLACE:

```typescript
  tollReceipts: {
    upload: string; entryPoint: string; exitPoint: string; vehicleClass: string
    searchPlaceholder: string
  }
```

WITH:

```typescript
  tollReceipts: {
    upload: string; entryPoint: string; exitPoint: string; vehicleClass: string
    searchPlaceholder: string
    // AddReceiptsModal
    addModalTitle: string; addModalSubtitle: string
    dropTitle: string; dropActive: string; dropHint: string
    filesSelected: string
    stepZipping: string; stepUploading: string; stepImporting: string; stepProcessing: string
    progressZipping: string; progressUploading: string; progressImporting: string; progressProcessing: string
    processingNote: string
    successTitle: string; successCount: string
    errorTitle: string
    btnAdd: string; btnUpload: string; btnDone: string; btnTryAgain: string; btnClose: string
    // ReceiptFilterDrawer
    filterTitle: string; anyDriver: string; anyStatus: string; capturedFrom: string; capturedTo: string
    // SendToAmazonModal
    sendTitle: string; sendSubtitle: string; sendFromDate: string; sendToDate: string
    sendPreviewBtn: string; sendDownloading: string; sendBtn: string; sendSending: string
    sendSuccess: string; sendSuccessMsg: string
    // Expense tab columns
    colChargeEx: string; colVat: string; colChargeInc: string; colProcessingStatus: string
  }
```
</action>

<acceptance_criteria>
- `lang-context.tsx` Translations type contains `fuelReceipts.addModalTitle`
- `lang-context.tsx` Translations type contains `tollReceipts.sendTitle`
- `lang-context.tsx` Translations type contains `fuelReceipts.filterTitle`
- TypeScript type is syntactically valid (no missing semicolons)
</acceptance_criteria>

---

## Task 3 — Add new namespaces to Translations type (fleetManagement, importHub, orgSettings, drivers extension)

<read_first>
- `apps/v4/components/lang-context.tsx` lines 215–336 (end of type block, before closing brace)
- `apps/v4/app/(dashboard)/fleet-management/page.tsx` (full file — grep for hardcoded strings first)
- `apps/v4/app/(dashboard)/import-hub/page.tsx` (full file)
- `apps/v4/app/(dashboard)/org-settings/page.tsx` (full file)
- `apps/v4/app/(dashboard)/drivers/page.tsx` (full file — identify missing keys not in t.common)
</read_first>

<action>
Grep each page for all hardcoded JSX strings (text between `>` and `</`, placeholder= values, aria-label= values, title= values, toast message strings in JS).

Then add new type sections BEFORE the closing `}` of the `Translations` type:

```typescript
  fleetManagement: {
    // Add type entries for all strings found in fleet-management/page.tsx
    // Example shape (fill with actual keys after grep):
    title: string; subtitle: string
    // ... all unique strings found
  }
  importHub: {
    // All strings from import-hub/page.tsx
    title: string
    // ... etc
  }
  orgSettings: {
    // All strings from org-settings/page.tsx  
    title: string
    // ... etc
  }
  drivers: {
    // Strings from drivers/page.tsx NOT already in t.common
    addTitle: string; editTitle: string
    nameLabel: string; statusLabel: string; licenceLabel: string
    depotLabel: string; phoneLabel: string; emailLabel: string
    deleteConfirm: string; deleteSuccess: string; deleteError: string
    saveSuccess: string; saveError: string
    // ... all remaining unique strings found
  }
```

The exact keys must match all hardcoded strings found via grep. Do not include strings already covered by `t.common.*` or `t.pages.*`.
</action>

<acceptance_criteria>
- `lang-context.tsx` Translations type contains `fleetManagement` namespace
- `lang-context.tsx` Translations type contains `importHub` namespace
- `lang-context.tsx` Translations type contains `orgSettings` namespace
- `lang-context.tsx` Translations type contains `drivers` namespace
- File contains `drivers: {` after existing type entries
</acceptance_criteria>

---

## Task 4 — Populate English translations for all new keys

<read_first>
- `apps/v4/components/lang-context.tsx` lines 519–540 (existing en.fuelReceipts and en.tollReceipts entries)
- The new type keys added in Tasks 2–3
</read_first>

<action>
In the `en` language object, REPLACE the minimal existing `fuelReceipts` and `tollReceipts` entries with full populated versions matching the new type shape:

**en.fuelReceipts** (find and expand):
```typescript
    fuelReceipts: {
      captured:"Captured", supplier:"Supplier", product:"Product",
      duplicate:"Duplicate", receipt:"Receipt", uploadZip:"Upload ZIP",
      searchPlaceholder:"Search by vehicle, supplier…",
      addModalTitle:"Add Fuel Receipts",
      addModalSubtitle:"Drop images or PDFs — they'll be zipped and sent for OCR automatically",
      dropTitle:"Drag & drop receipts here", dropActive:"Drop to add",
      dropHint:"or click to browse · JPEG, PNG, WebP, PDF",
      filesSelected:"{n} file{s} selected",
      stepZipping:"Zipping", stepUploading:"Uploading",
      stepImporting:"Importing", stepProcessing:"Processing OCR",
      progressZipping:"Zipping {n} file{s}…", progressUploading:"Uploading to server…",
      progressImporting:"Importing receipt records…", progressProcessing:"Running OCR processing…",
      processingNote:"This may take a moment for large batches",
      successTitle:"Upload & processing complete", successCount:"{n} receipt{s} imported",
      errorTitle:"Upload failed",
      btnAdd:"Add", btnUpload:"Upload {n} file{s}", btnDone:"Done — Refresh List",
      btnTryAgain:"Try Again", btnClose:"Close",
      filterTitle:"Filter Receipts", anyDriver:"Any driver", anyStatus:"Any status",
      capturedFrom:"Captured from", capturedTo:"Captured to",
      detailTitle:"Fuel Receipt Detail", ocrData:"OCR Extracted Data", rawText:"Raw text",
      colOcrStatus:"OCR Status", colCapturedAt:"Captured At",
      colSupplier:"Supplier", colProduct:"Product", colVolume:"Volume",
    },
```

**en.tollReceipts** (find and expand):
```typescript
    tollReceipts: {
      upload:"Upload", entryPoint:"Entry Point", exitPoint:"Exit Point",
      vehicleClass:"Vehicle Class", searchPlaceholder:"Search vehicle, route…",
      addModalTitle:"Add Toll Receipts",
      addModalSubtitle:"Drop images or PDFs — they'll be zipped and sent for OCR automatically",
      dropTitle:"Drag & drop receipts here", dropActive:"Drop to add",
      dropHint:"or click to browse · JPEG, PNG, WebP, PDF",
      filesSelected:"{n} file{s} selected",
      stepZipping:"Zipping", stepUploading:"Uploading",
      stepImporting:"Importing", stepProcessing:"Processing OCR",
      progressZipping:"Zipping {n} file{s}…", progressUploading:"Uploading to server…",
      progressImporting:"Importing receipt records…", progressProcessing:"Running OCR processing…",
      processingNote:"This may take a moment for large batches",
      successTitle:"Upload & processing complete", successCount:"{n} receipt{s} imported",
      errorTitle:"Upload failed",
      btnAdd:"Add", btnUpload:"Upload {n} file{s}", btnDone:"Done — Refresh List",
      btnTryAgain:"Try Again", btnClose:"Close",
      filterTitle:"Filter Receipts", anyDriver:"Any driver", anyStatus:"Any status",
      capturedFrom:"Captured from", capturedTo:"Captured to",
      sendTitle:"Send to Amazon", sendSubtitle:"Submit toll charges to Amazon Relay for the selected period",
      sendFromDate:"From Date", sendToDate:"To Date",
      sendPreviewBtn:"Preview data before sending", sendDownloading:"Downloading…",
      sendBtn:"Send to Amazon Relay", sendSending:"Sending…",
      sendSuccess:"Submitted successfully",
      sendSuccessMsg:"Toll charges have been sent to Amazon Relay.",
      colChargeEx:"Charge (ex. VAT)", colVat:"VAT",
      colChargeInc:"Charge (inc. VAT)", colProcessingStatus:"Processing Status",
    },
```

Then add the new `fleetManagement`, `importHub`, `orgSettings`, `drivers` English entries with values matching all strings found in Task 3.
</action>

<acceptance_criteria>
- `lang-context.tsx` file contains `addModalTitle:"Add Fuel Receipts"`
- `lang-context.tsx` file contains `addModalTitle:"Add Toll Receipts"`
- `lang-context.tsx` file contains `sendTitle:"Send to Amazon"`
- `lang-context.tsx` file contains `filterTitle:"Filter Receipts"`
- `lang-context.tsx` file contains `btnDone:"Done — Refresh List"`
</acceptance_criteria>

---

## Task 5 — Populate non-English translations (de/fr/es/it/pl) for all new keys

<read_first>
- `apps/v4/components/lang-context.tsx` — find the `de:` block, `fr:` block, `es:` block, `it:` block, `pl:` block
- The new English values added in Task 4 (use as source for translation)
</read_first>

<action>
For each language (de, fr, es, it, pl), find its `fuelReceipts` and `tollReceipts` entries and expand them with the same new keys, using appropriate translations:

**German (de) examples:**
- `addModalTitle:"Kraftstoffbelege hinzufügen"` / `"Mautbelege hinzufügen"`
- `dropTitle:"Belege hierher ziehen"`, `dropActive:"Ablegen zum Hinzufügen"`
- `dropHint:"oder klicken zum Durchsuchen · JPEG, PNG, WebP, PDF"`
- `stepZipping:"Komprimieren"`, `stepUploading:"Hochladen"`, `stepImporting:"Importieren"`, `stepProcessing:"OCR-Verarbeitung"`
- `progressZipping:"Komprimiere {n} Datei{s}…"`, `progressUploading:"Wird auf Server hochgeladen…"`
- `successTitle:"Upload & Verarbeitung abgeschlossen"`, `errorTitle:"Upload fehlgeschlagen"`
- `btnDone:"Fertig — Liste aktualisieren"`, `filterTitle:"Belege filtern"`
- `sendTitle:"An Amazon senden"`, `anyDriver:"Beliebiger Fahrer"`, `anyStatus:"Beliebiger Status"`

**French (fr) examples:**
- `addModalTitle:"Ajouter des reçus de carburant"` / `"Ajouter des reçus de péage"`
- `dropTitle:"Glisser-déposer les reçus ici"`, `dropActive:"Déposer pour ajouter"`
- `stepZipping:"Compression"`, `stepUploading:"Téléchargement"`, `stepImporting:"Importation"`, `stepProcessing:"Traitement OCR"`
- `successTitle:"Téléchargement et traitement terminés"`, `errorTitle:"Échec du téléchargement"`
- `btnDone:"Terminé — Actualiser la liste"`, `filterTitle:"Filtrer les reçus"`
- `sendTitle:"Envoyer à Amazon"`

**Spanish (es) examples:**
- `addModalTitle:"Agregar recibos de combustible"` / `"Agregar recibos de peaje"`
- `dropTitle:"Arrastrar y soltar recibos aquí"`, `dropActive:"Soltar para agregar"`
- `stepZipping:"Comprimiendo"`, `stepUploading:"Subiendo"`, `stepImporting:"Importando"`, `stepProcessing:"Procesando OCR"`
- `successTitle:"Subida y procesamiento completos"`, `errorTitle:"Error en la subida"`
- `btnDone:"Listo — Actualizar lista"`, `filterTitle:"Filtrar recibos"`

**Italian (it) examples:**
- `addModalTitle:"Aggiungi ricevute carburante"` / `"Aggiungi ricevute pedaggi"`
- `dropTitle:"Trascina e rilascia le ricevute qui"`, `dropActive:"Rilascia per aggiungere"`
- `stepZipping:"Compressione"`, `stepUploading:"Caricamento"`, `stepImporting:"Importazione"`, `stepProcessing:"Elaborazione OCR"`
- `successTitle:"Caricamento ed elaborazione completati"`, `errorTitle:"Caricamento non riuscito"`
- `btnDone:"Fatto — Aggiorna lista"`, `filterTitle:"Filtra ricevute"`

**Polish (pl) examples:**
- `addModalTitle:"Dodaj paragony za paliwo"` / `"Dodaj paragony za przejazd"`
- `dropTitle:"Przeciągnij i upuść paragony tutaj"`, `dropActive:"Upuść aby dodać"`
- `stepZipping:"Kompresja"`, `stepUploading:"Przesyłanie"`, `stepImporting:"Importowanie"`, `stepProcessing:"Przetwarzanie OCR"`
- `successTitle:"Przesyłanie i przetwarzanie zakończone"`, `errorTitle:"Przesyłanie nie powiodło się"`
- `btnDone:"Gotowe — Odśwież listę"`, `filterTitle:"Filtruj paragony"`

Also translate the `fleetManagement`, `importHub`, `orgSettings`, `drivers` namespaces for all 5 non-English languages.

Commit: `feat(i18n-999.9): extend lang-context with all new translation keys (all 6 languages)`
</action>

<acceptance_criteria>
- `lang-context.tsx` contains `"Kraftstoffbelege hinzufügen"` (German fuel modal title)
- `lang-context.tsx` contains `"Ajouter des reçus de carburant"` (French fuel modal title)
- `lang-context.tsx` file has no TypeScript type errors when checked with tsc
- All 6 language blocks contain `addModalTitle` in both `fuelReceipts` and `tollReceipts`
- Running `npx tsc --noEmit --project apps/v4/tsconfig.json` shows only pre-existing errors (not new ones from this change)
</acceptance_criteria>

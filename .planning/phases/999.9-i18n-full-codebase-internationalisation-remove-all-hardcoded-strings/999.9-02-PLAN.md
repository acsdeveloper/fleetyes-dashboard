---
id: "999.9-02"
wave: 2
autonomous: true
depends_on: ["999.9-01"]
objective: "Replace all hardcoded English strings in the highest-priority pages (toll/page.tsx, fuel/page.tsx, compliance/page.tsx, trips/page.tsx) with t.xxx references from the extended lang-context."
files_modified:
  - apps/v4/app/(dashboard)/toll/page.tsx
  - apps/v4/app/(dashboard)/fuel/page.tsx
  - apps/v4/app/(dashboard)/compliance/page.tsx
  - apps/v4/app/(dashboard)/trips/page.tsx
task_count: 4
---

# Plan 999.9-02: Replace Hardcoded Strings — High Priority Pages

## Objective

Replace hardcoded English strings in the 4 pages with the most hardcoded
strings. All pages already import and use `useLang()` — this plan wires up
the remaining gaps using the new keys added in Plan 999.9-01.

**Dependency:** Plan 999.9-01 must be complete (all new t.xxx keys must exist).

---

## Task 1 — Replace hardcoded strings in toll/page.tsx

<read_first>
- `apps/v4/app/(dashboard)/toll/page.tsx` (full file)
- `apps/v4/components/lang-context.tsx` lines 519–535 (en.tollReceipts, en.fuelReceipts)
</read_first>

<action>
The toll page already has `const { t } = useLang()` in the main page component.

Update each sub-component (`AddReceiptsModal`, `ReceiptFilterDrawer`, `SendToAmazonModal`, `ExpenseFilterDrawer`, `TollExpenseDetailDrawer`) to call `const { t } = useLang()` at the top if they don't already.

Then replace every hardcoded string with the corresponding `t.tollReceipts.xxx` or `t.common.xxx` key:

**AddReceiptsModal replacements:**
- `"Add Toll Receipts"` → `t.tollReceipts.addModalTitle`
- `"Drop images or PDFs — they'll be zipped..."` → `t.tollReceipts.addModalSubtitle`
- `"Drag & drop receipts here"` → `t.tollReceipts.dropTitle`
- `"Drop to add"` → `t.tollReceipts.dropActive`
- `"or click to browse · JPEG, PNG, WebP, PDF"` → `t.tollReceipts.dropHint`
- `` `${files.length} file${files.length !== 1 ? "s" : ""} selected` `` → `` `${files.length} ${t.tollReceipts.filesSelected}` `` (or adjust template as needed)
- `"Zipping"` → `t.tollReceipts.stepZipping`
- `"Uploading"` → `t.tollReceipts.stepUploading`
- `"Importing"` → `t.tollReceipts.stepImporting`
- `"Processing OCR"` → `t.tollReceipts.stepProcessing`
- `` `Zipping ${files.length} file${...}…` `` → `` `${t.tollReceipts.progressZipping.replace("{n}", String(files.length)).replace("{s}", files.length !== 1 ? "s" : "")}` ``
- `"Uploading to server…"` → `t.tollReceipts.progressUploading`
- `"Importing receipt records…"` → `t.tollReceipts.progressImporting`
- `"Running OCR processing…"` → `t.tollReceipts.progressProcessing`
- `"This may take a moment for large batches"` → `t.tollReceipts.processingNote`
- `"Upload & processing complete"` → `t.tollReceipts.successTitle`
- `` `${importedCount} receipt${importedCount !== 1 ? "s" : ""} imported` `` → `t.tollReceipts.successCount` (interpolate count)
- `"Upload failed"` → `t.tollReceipts.errorTitle`
- `"Cancel"` → `t.common.cancel`
- `` `Upload ${files.length > 0 ? `${files.length} file${…}` : ""}` `` → use `t.tollReceipts.btnUpload`
- `"Done — Refresh List"` → `t.tollReceipts.btnDone`
- `"Try Again"` → `t.tollReceipts.btnTryAgain`
- `"Close"` → `t.common.close`

**ReceiptFilterDrawer replacements:**
- `"Filter Receipts"` → `t.tollReceipts.filterTitle`
- `"Driver"` → `t.common.driver`
- `"Any driver"` → `t.tollReceipts.anyDriver`
- `"Status"` → `t.common.status`
- `"Any status"` → `t.tollReceipts.anyStatus`
- `"Captured from"` → `t.tollReceipts.capturedFrom`
- `"Captured to"` → `t.tollReceipts.capturedTo`
- `"Clear all"` → `t.common.clearAll`
- `"Apply"` → `t.common.apply`

**SendToAmazonModal replacements:**
- `"Send to Amazon"` → `t.tollReceipts.sendTitle`
- `"Submit toll charges to Amazon Relay..."` → `t.tollReceipts.sendSubtitle`
- `"From Date"` → `t.tollReceipts.sendFromDate`
- `"To Date"` → `t.tollReceipts.sendToDate`
- `"Preview data before sending"` → `t.tollReceipts.sendPreviewBtn`
- `"Downloading…"` → `t.tollReceipts.sendDownloading`
- `"Send to Amazon Relay"` → `t.tollReceipts.sendBtn`
- `"Sending…"` → `t.tollReceipts.sendSending`
- `"Submitted successfully"` → `t.tollReceipts.sendSuccess`

**Expense tab column headers** (in the ExpensesTab table):
- `"Charge (ex. VAT)"` → `t.tollReceipts.colChargeEx`
- `"VAT"` → `t.tollReceipts.colVat`
- `"Charge (inc. VAT)"` → `t.tollReceipts.colChargeInc`
- `"Processing Status"` → `t.tollReceipts.colProcessingStatus`

Commit: `feat(i18n-999.9): replace hardcoded strings in toll page`
</action>

<acceptance_criteria>
- `toll/page.tsx` does NOT contain the string literal `"Add Toll Receipts"` as JSX text
- `toll/page.tsx` does NOT contain `"Filter Receipts"` as JSX text
- `toll/page.tsx` does NOT contain `"Done — Refresh List"` as JSX text
- `toll/page.tsx` does NOT contain `"Send to Amazon"` as JSX text (inside the modal component)
- `toll/page.tsx` does NOT contain `"Upload & processing complete"` as JSX text
- `toll/page.tsx` contains `t.tollReceipts.addModalTitle`
- `toll/page.tsx` contains `t.tollReceipts.filterTitle`
</acceptance_criteria>

---

## Task 2 — Replace hardcoded strings in fuel/page.tsx

<read_first>
- `apps/v4/app/(dashboard)/fuel/page.tsx` (full file)
- `apps/v4/components/lang-context.tsx` lines 519–524 (en.fuelReceipts)
</read_first>

<action>
Same pattern as Task 1, but using `t.fuelReceipts.xxx` keys. The fuel page's
`AddReceiptsModal` and `ReceiptFilterDrawer` are structurally identical to
toll's but use fuel-specific API calls. The string replacements are the same
except the modal title:

**AddReceiptsModal replacements:**
- `"Add Fuel Receipts"` → `t.fuelReceipts.addModalTitle`
- All other AddReceiptsModal strings → same pattern as toll but `t.fuelReceipts.xxx`

**ReceiptFilterDrawer replacements:**
- Same keys as toll but `t.fuelReceipts.filterTitle`, `t.fuelReceipts.anyDriver`, etc.

**FuelExpenseTab** (if it has hardcoded column headers not already in t.fuelTracking):
- Check and replace any remaining hardcoded column headers with appropriate keys

**FuelReceiptDetailDrawer:**
- `"Fuel Receipt Detail"` → `t.fuelReceipts.detailTitle`
- `"OCR Extracted Data"` → `t.fuelReceipts.ocrData`
- `"OCR Status"` → `t.fuelReceipts.colOcrStatus`
- `"Captured At"` → `t.fuelReceipts.colCapturedAt`
- etc.

Commit: `feat(i18n-999.9): replace hardcoded strings in fuel page`
</action>

<acceptance_criteria>
- `fuel/page.tsx` does NOT contain `"Add Fuel Receipts"` as JSX text
- `fuel/page.tsx` does NOT contain `"Filter Receipts"` as JSX text
- `fuel/page.tsx` does NOT contain `"Upload & processing complete"` as JSX text
- `fuel/page.tsx` does NOT contain `"Done — Refresh List"` as JSX text
- `fuel/page.tsx` contains `t.fuelReceipts.addModalTitle`
</acceptance_criteria>

---

## Task 3 — Replace hardcoded strings in compliance/page.tsx

<read_first>
- `apps/v4/app/(dashboard)/compliance/page.tsx` (full file)
- `apps/v4/components/lang-context.tsx` — the `complianceHub` namespace starts around line 249
</read_first>

<action>
The compliance page already uses `useLang()`. Identify all remaining hardcoded
strings NOT yet routed through `t.complianceHub.xxx` or `t.common.xxx`.

Common gaps in compliance pages:
- Modal/drawer titles and description text
- Inline status labels in JSX
- Filter drawer labels
- Confirmation dialog text
- Toast message strings (e.g. passed to `toast({ description: "..." })`)

**Pattern for toast strings:**
```typescript
// Before:
toast({ description: "Changes saved successfully" })
// After (add key to t.complianceHub or t.common if generic):
toast({ description: t.complianceHub.allSettingsSaved })
```

Replace all hardcoded strings found with appropriate existing `t.complianceHub.xxx`
keys. If a string has no matching key in complianceHub, check `t.common.*` first.
If still not covered, add the key to `t.complianceHub` in lang-context.tsx and
populate all 6 languages.

Commit: `feat(i18n-999.9): replace hardcoded strings in compliance page`
</action>

<acceptance_criteria>
- `compliance/page.tsx` hardcoded string count reduced significantly
- Any new keys added to lang-context.tsx have all 6 languages populated
- No new TypeScript errors introduced
</acceptance_criteria>

---

## Task 4 — Replace hardcoded strings in trips/page.tsx

<read_first>
- `apps/v4/app/(dashboard)/trips/page.tsx` (full file)
- `apps/v4/components/lang-context.tsx` — `t.trips` namespace and `t.common`
</read_first>

<action>
The trips page uses `useLang()`. Identify and replace remaining hardcoded strings:

Common gaps:
- Column header strings not in `t.trips.*` or `t.common.*`
- Filter drawer field labels (Date From, Date To, Status, Driver, Vehicle, etc.)
- Edit/create drawer field labels
- Status pills text
- Empty state messages
- Pagination text

**For filter drawer labels not in t.common:**
Add to `t.trips` namespace if trips-specific, or use `t.common.xxx` if generic.

Replace all identified instances with `t.trips.xxx` or `t.common.xxx`.

If new keys needed, add them to lang-context.tsx with all 6 language values.

Commit: `feat(i18n-999.9): replace hardcoded strings in trips page`
</action>

<acceptance_criteria>
- `trips/page.tsx` hardcoded string count reduced
- No new TypeScript errors
- Any new lang-context keys have all 6 languages populated
</acceptance_criteria>

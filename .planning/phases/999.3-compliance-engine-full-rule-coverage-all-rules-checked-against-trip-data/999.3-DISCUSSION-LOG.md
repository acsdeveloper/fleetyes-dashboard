# Phase 999.3: Compliance Engine — Full Rule Coverage — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-01
**Phase:** 999.3-compliance-engine-full-rule-coverage-all-rules-checked-against-trip-data
**Areas discussed:** No-data behaviour, Missing end time handling, Working time definition, Test coverage requirements

---

## A — No-Data / Clean-Slate Driver

| Option | Description | Selected |
|--------|-------------|----------|
| Treat as resting | No trips = driver was free. `buildRestDay()` for all empty days. | ✓ |
| Treat as gap / unknown | No data = unknown state → flag record-keeping issue | |

**User's choice:** Treat as resting. New business or no recorded history = driver was free in that period.
**Notes:** Record-keeping soft warning (29-day window incomplete) is acceptable and factually correct. No change to current adapter behaviour.

---

## B — Missing End Time Handling

| Option | Description | Selected |
|--------|-------------|----------|
| Hard block at assignment | MISSING_END_TIME violation blocks the drop | (was previous behaviour) |
| Remove block, flag in Trips list | No block at assignment; amber "Risk: No end time" badge on trip row in Trips page | ✓ |
| Warn at assignment but allow | Non-blocking warning shown in compliance panel | |

**User's choice:** Remove the assignment-time block entirely. Flag missing end time as a data quality risk in the Trips list UI where the user can see and fix it.
**Notes:** "There will be no missing end time [in production]." The 2h fallback in `tripEnd()` remains as a safety net for gap calculation. The MISSING_END_TIME test must be updated to reflect new behaviour.

---

## C — Working Time Definition & Break Assumptions

| Option | Description | Selected |
|--------|-------------|----------|
| All trip time = working | scheduled_at → end = total work window; breaks assumed within | ✓ |
| Track driving hours separately | Separate DRIVING vs DUTY activities with explicit breaks | (future) |

**User's choice:** All time between start and end = working time. For this implementation, assume drivers take compliant breaks within the working window.
**Notes:** Implement `breaksAssumedCompliant: true` flag in VehicleConfig now but don't change behaviour. Future mode (`false`) will enforce break timing when driving timestamps are tracked separately. Critical fix: `if (!day.hasDriving) continue` in `assimilated.ts` must become `if (day.isRestDay) continue` so the daily work limit checks non-driving working days too.

---

## D — Test Coverage

| Option | Description | Selected |
|--------|-------------|----------|
| Prioritise specific rules | Cover only the most common violations | |
| Cover everything if data exists | All 8 rules tested; no-data = no violation | ✓ |

**User's choice:** If we have data, we check everything. If we don't have data, it's fine (no violation).
**Notes:** Missing tests to add: weekly rest (45h/24h), fortnightly driving (90h), reduced rest limit (3×/week), daily work limit on NON_DRIVING_DUTY days, clean-slate driver scenario. All 35 existing tests must still pass. Zero deployment until full suite is green.

---

## Agent's Discretion

- `breaksAssumedCompliant` placement (VehicleConfig vs engine-level option)
- Exact styling of "Risk: No end time" badge in Trips list
- Whether new no-data test goes in a new file or appended to an existing test file

## Deferred Ideas

- `breaksAssumedCompliant: false` enforcement mode (future phase when break timestamps tracked)
- International driving detection and testing
